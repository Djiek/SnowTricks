{% extends 'base.html.twig' %}

{% form_theme formFigure 'bootstrap_4_layout.html.twig' %}

{% block title %}Ajouter une figure{% endblock %}

{% block body %}
<br/>
<section id="spaceShowFigure">
   </section>
<section id="showFigure">
<img src= "{{ asset('images/snowboard-extreme-sports.jpeg') }}" alt="supprimer"/>
         <div class="hero-container" data-aos="fade-up">
         <div class="container">
           <div  class="section-title" data-aos="fade-in" data-aos-delay="100">
          {% if editMode %}
     <h2>Modifier la figure</h2>
    {% else %}
     <h2>Page de création de figure de snowboard</h2>
     {% endif %}
        </div>
    </div>
<p id="smallSpace"></p>
{{ form_start(formFigure) }}

    {{ form_row(formFigure.name, { 'require':'true','label':'Nom de la figure : '})}}
    {{ form_row(formFigure.description, { 'require':'true', 'label':'Description : '})}}
    {{ form_row(formFigure.tricksGroup, { 'require':'true', 'label':'Niveau : '})}}
   

  {# {% for image in formFigure.images %} #}
     {# <ul class="tags" data-prototype="{{ form_widget(formFigure.images.vars.prototype)|e('html_attr') }}"></ul>
          <button type="button" class="add_item_link" data-collection-holder-class="tags">Ajouter un upload</button>{{ form_row(formFigure.images, {'label':'Image(s) '})}} #}
     <div class="bordureVideo">  {{ form_row(formFigure.images, {'label':'Image(s) '})}}   </div>
       {# {% endfor %} #}
<br/>
    <div class="bordureVideo"><br/> <h6> Video(s) </h6 ><small>(Pour le lien video youtube, veuillez aller dans "partager" puis "integer la video" et copier le lien de la video. [Ex : https://www.youtube.com/embed/5mtNg2mf-Hg] )</small>  {{ form_row(formFigure.videos, {'label':false})}} </div>
   <br/>

    {% if editMode %}
    <h2>Image(s)</h2>
    {% for image in figure.images %}
        <div>
            <img src="{{ asset('/assets/uploads/' ~ image.link) }}" alt="image" width="150">
            <a href="{{ path('image_deleted', {id: image.id})}}" data-delete data-token="{{ csrf_token('delete'~ image.id)}}"> <img src= "{{ asset('images/poubelle.jpg') }}" alt="supprimer"/></a> </a>
        </div>

    {% endfor %}
 


   {% endif %}

   {% if editMode %}
     <a class="logosUpdateDelete btn btn-danger" onClick="return confirm('Voulez-vous vraiment supprimer ce post ?')" href="{{ path('figure_deleted', {'id': figure.id}) }}"> 
      Suprimer la figure
      </a>
      {% endif %}

    <button type="submit" class="btn btn-success">
    {% if editMode %}
    Enregistrer les modifications
    {% else %}
     Ajouter la figure
     {% endif %}
</button>
    {{ form_end(formFigure) }}
<br/>




{% endblock %}